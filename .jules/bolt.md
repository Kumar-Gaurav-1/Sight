## 2026-08-08 - DateFormatter instance creation in loops\n**Learning:** Instantiating DateFormatter (especially ISO8601DateFormatter) inside .map closures is a common performance anti-pattern. This is computationally expensive, as each DateFormatter instantiation is costly.\n**Action:** Extract DateFormatter creation outside of loops and map closures and reuse the single instance to optimize performance.
