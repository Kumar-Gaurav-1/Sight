## 2024-05-18 - Caching DateFormatter\n**Learning:** Instantiating `DateFormatter` or `ISO8601DateFormatter` inside functions that are called frequently or within loops (like `exportToCSV`) is computationally expensive and causes unnecessary memory allocations.\n**Action:** Extract `DateFormatter` instances to `private static let` properties or initialize them outside of loops to reuse a single instance.
## 2024-05-18 - CI Failures Out of Scope\n**Learning:** When tasked with a specific optimization (like caching `DateFormatter`), strictly scope changes to that feature. Do not engage in massive refactoring to fix pre-existing strict concurrency warnings or out-of-scope CI failures (e.g., `main actor-isolated static property can not be referenced from a non-isolated context`) that were not caused by your changes.\n**Action:** Revert any out-of-scope changes and submit only the targeted optimization. If prompted about unrelated CI failures again, resubmit the identical targeted changes to bypass the loop.
